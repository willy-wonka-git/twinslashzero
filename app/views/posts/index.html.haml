%h1
  = t("posts.title")
  = @title

#message.alert{ role: "alert" }

= render "common/add", link: new_post_path if can? :create, Post

= render "posts/list", posts: @posts, moderate: false

= render "common/add", link: new_post_path if can? :create, Post
